/** Вариант 7: размерность регистра = степень многочлена 29 */

export const VARIANT = 7;
export const REGISTER_LENGTH = 29;

/** Примитивный многочлен: x^29 + x^2 + 1 */
export const POLYNOMIAL_LABEL = 'x^29 + x^2 + 1';

/**
 * Отводы регистра (индексы ячеек 0…n−1, младший разряд — 0).
 *
 * Для многочлена x^n + x^k + 1 в схеме с обратной связью (как в методичке):
 *   — член «+1» → ячейка 0;
 *   — член x^k → ячейка (n − k), здесь k=2 → 29−2 = 27.
 *
 * Не путать с записью «индекс = k»: тогда для x^2 получилось бы [0, 2],
 * что соответствовало бы другому многочлену x^29 + x^27 + 1 при той же нумерации.
 */
export const FEEDBACK_TAPS = [0, 27];
